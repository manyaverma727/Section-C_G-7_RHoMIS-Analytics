# Claude context (never commit)
CLAUDE.md

# macOS
.DS_Store
.AppleDouble
.LSOverride

# AI workflow context (local only)
.ai-workflow/

# Reference template (local only)
reference/

# Raw data — large files, not tracked in git
data/raw/*.csv

# Processed outputs — regenerated by pipeline
data/processed/*.csv

# Jupyter
.ipynb_checkpoints/
**/.ipynb_checkpoints/

# Python
__pycache__/
*.py[cod]
*.pyo
.env
venv/
.venv/
*.egg-info/
dist/
build/

# Reports/presentations (binary, committed manually when final)
# Uncomment to exclude:
# reports/*.pdf

# Tableau workbooks (committed manually)
# tableau/*.twbx
